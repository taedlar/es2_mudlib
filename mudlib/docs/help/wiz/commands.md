# 巫師指令列表 (依字母順序)

```text
cat  cd  clone  cp  dest  force  goto  home  info  ls  mkdir  more  mv
 promote  pwd  reboot  rm  rmdir  shutdown  tail  update  where  which  wizlock
```

玩家指令請改用 `help player/commands`
